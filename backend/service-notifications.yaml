apiVersion: v1
kind: Service
metadata:
  name: notifications-svc
  namespace: taskflow-backend
  labels:
    app: notifications
spec:
  type: ClusterIP
  selector:
    app: notifications
  ports:
  - port: 3001
    targetPort: 3001
    name: http
