apiVersion: v1
kind: Service
metadata:
  name: metrics-svc
  namespace: taskflow-backend
  labels:
    app: metrics
spec: 
  type: ClusterIP
  selector:
    app: metrics
  ports:
  - port: 5001
    targetPort: 5001
    name: http
